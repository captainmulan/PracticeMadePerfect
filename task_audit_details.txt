FILE: csharp-api-json-deserialization.json
 title: Api Call and JSON Deserialization
 desc: Write an async method that calls an API and deserializes the JSON response into a generic object.
 checklist: ['Create an async method that accepts a URL', 'Use HttpClient to perform a GET request', 'Read the response content as a string', 'Deserialize JSON into type T', 'Throw an exception when the request fails']
 starterCode: True
 starterCode snippet:
// 1: Create an async generic method accepting apiUrl\n// 2: Use HttpClient to call the endpoint\n// 3: Read the response content\n// 4: Deserialize JSON into type T\n// 5: Throw when the call fails
 hints count: 6
  hint1 guide: // guide hints: Define the async generic method
   code: public async Task<T> GetData<T>(string apiUrl)\n{
  hint2 guide: // guide hints: Send the HTTP GET request
   code:     HttpResponseMessage response = await _httpClient.GetAsync(apiUrl);
  hint3 guide: // guide hints: Read the response body as a string
   code:     string json = await response.Content.ReadAsStringAsync();
  hint4 guide: // guide hints: Deserialize the JSON into T
   code:     T data = JsonConvert.DeserializeObject<T>(json);
  hint5 guide: // guide hints: Throw if the response is not successful
   code:     if (!response.IsSuccessStatusCode)\n    {\n        throw new Exception("Failed");\n    }
  hint6 guide: // guide hints: Return the deserialized object
   code:     return data;\n}

FILE: csharp-bouncing-ball.json
 title: Bounce Count and Distance
 desc: Given an initial height, calculate the total distance traveled by a bouncing ball and the number of bounces until it stops.
 checklist: ['Initialize bounce counter to 0', 'Initialize distance to 0', 'Create while loop for ball bouncing', 'Add up distance from down and up movement', 'Calculate bounce height (usually 2/3 or similar)', 'Stop when height becomes negligible']
 starterCode: False
 hints count: 6
  hint1 guide: // guide hints: Import: using System; and Collections
   code: using System;\nusing System.Collections.Generic;
  hint2 guide: // guide hints: Create a method that accepts initial height and bounce factor
   code: public static (int bounces, double distance) CalculateBounce(double height, double factor = 0.66, double threshold = 0.01)\n{
  hint3 guide: // guide hints: Initialize counters for bounces and distance
   code:     int bounces = 0;\n    double distance = 0;\n    double current = height;
  hint4 guide: // guide hints: Add initial fall distance and loop while current > threshold
   code:     if (current <= 0) return (0, 0);\n    distance += current;\n    while (current > threshold)\n    {
  hint5 guide: // guide hints: Compute next bounce height, add up down+up distances, increment bounces
   code:         current = current * factor;\n        distance += 2 * current;\n        bounces++;\n    }
  hint6 guide: // guide hints: Return bounces and total distance
   code:     return (bounces, distance);\n}

FILE: csharp-bubble-sort.json
 title: Bubble Sort Implementation
 desc: Write a C# method that sorts an integer array in ascending order using bubble sort.
 checklist: ['Create outer loop (i) to iterate through array', 'Create inner loop (j) to compare adjacent elements', 'Add comparison logic (if items[j] > items[j+1])', 'Implement swap logic using temp variable', 'Return sorted array']
 starterCode: True
 starterCode snippet:
// 1: Create outer loop (i) to iterate through the array\n// 2: Create inner loop (j) to compare adjacent elements\n// 3: If items[j] > items[j+1], swap using a temp variable\n// 4: Continue passes until sorted\n// 5: Return the sorted array
 hints count: 5
  hint1 guide: // guide hints: Define the bubble sort method signature
   code: public static int[] BubbleSort(int[] items)\n{
  hint2 guide: // guide hints: Outer loop for passes
   code:     int n = items.Length;\n    for (int i = 0; i < n - 1; i++)\n    {
  hint3 guide: // guide hints: Inner loop to compare adjacent elements
   code:         for (int j = 0; j < n - i - 1; j++)\n        {
  hint4 guide: // guide hints: Swap when items[j] > items[j+1]
   code:             if (items[j] > items[j + 1])\n            {\n                int temp = items[j];\n                items[j] = items[j + 1];\n                items[j + 1] = temp;\n            }
  hint5 guide: // guide hints: Close loops and return sorted array
   code:         }\n    }\n    return items;\n}

FILE: csharp-character-count.json
 title: Character Count in a String
 desc: Count the occurrence of each character in a string, ignoring spaces.
 checklist: ['Accept a string parameter', 'Iterate through each character', 'Ignore spaces while counting', 'Use a Dictionary<char, int> to store counts', 'Return the dictionary of character counts']
 starterCode: True
 starterCode snippet:
// 1: Accept a string input\n// 2: Use a Dictionary<char, int> to count characters\n// 3: Ignore spaces while counting\n// 4: Return the character counts
 hints count: 4
  hint1 guide: // guide hints: Define the character count method
   code: public static Dictionary<char, int> CountCharacters(string input)\n{
  hint2 guide: // guide hints: Create the dictionary to store counts
   code:     Dictionary<char, int> charCount = new Dictionary<char, int>();
  hint3 guide: // guide hints: Iterate through each character and skip spaces
   code:     foreach (char c in input)\n    {\n        if (c != ' ')\n        {\n            if (charCount.ContainsKey(c))\n            {\n                charCount[c]++;\n            }\n            else\n            {\n                charCount[c] = 1;\n            }\n        }\n    }
  hint4 guide: // guide hints: Return the character count dictionary
   code:     return charCount;\n}

FILE: csharp-dependency-injection.json
 title: Dependency Injection Example
 desc: Implement a simple service and client class to demonstrate constructor dependency injection.
 checklist: ['Create a Service class with an operation method', 'Create a Client class that accepts Service in the constructor', 'Store the injected service in a private readonly field', 'Call the service operation from the client', 'Return or use the service result']
 starterCode: True
 starterCode snippet:
// 1: Create a Service class with PerformOperation\n// 2: Create a Client class that receives Service through constructor injection\n// 3: Store the service in a readonly field\n// 4: Call the service method from the client
 hints count: 3
  hint1 guide: // guide hints: Define the service class
   code: public class Service\n{\n    public void PerformOperation()\n    {\n        // Operation implementation\n    }\n}
  hint2 guide: // guide hints: Define the client class with injected service
   code: public class Client\n{\n    private readonly Service _service;\n\n    public Client(Service service)\n    {\n        _service = service;\n    }
  hint3 guide: // guide hints: Use the injected service in a method
   code:     public void UseService()\n    {\n        _service.PerformOperation();\n    }\n}

FILE: csharp-exception-handling.json
 title: C# Exception Handling
 desc: Implement a method that divides two integers and demonstrates correct exception handling with specific catch blocks.
 checklist: ['Create a method that accepts dividend and divisor', 'Use try/catch around the division operation', 'Catch DivideByZeroException specifically', 'Add a general Exception catch block for other failures', 'Return an error message or result based on success']
 starterCode: True
 starterCode snippet:
// 1: Create a method that accepts dividend and divisor\n// 2: Wrap the division in try/catch blocks\n// 3: Catch DivideByZeroException first\n// 4: Catch Exception afterward\n// 5: Return a success or error message
 hints count: 6
  hint1 guide: // guide hints: Define the method signature
   code: public static string DivideWithHandling(int dividend, int divisor)\n{
  hint2 guide: // guide hints: Start a try block for dividing the values
   code:     try\n    {\n        int result = dividend / divisor;
  hint3 guide: // guide hints: Return the successful result string
   code:         return $"Result: {result}";\n    }
  hint4 guide: // guide hints: Catch DivideByZeroException explicitly
   code:     catch (DivideByZeroException ex)\n    {\n        return "Error: Cannot divide by zero";\n    }
  hint5 guide: // guide hints: Catch any other exceptions afterward
   code:     catch (Exception ex)\n    {\n        return $"Error: {ex.Message}";\n    }
  hint6 guide: // guide hints: Close the method
   code: }

FILE: csharp-first-duplicate.json
 title: Find First Duplicate
 desc: Create a method that returns the first duplicate value found in an integer array.
 checklist: ['Accept an integer array parameter', 'Use a HashSet to track seen values', 'Iterate through the array elements', 'Return the first repeated value', 'Return -1 if no duplicate is found']
 starterCode: True
 starterCode snippet:
// 1: Accept an integer array\n// 2: Use a HashSet<int> for seen values\n// 3: Return the first value that already exists in the set\n// 4: Return -1 when no duplicate exists
 hints count: 5
  hint1 guide: // guide hints: Define the duplicate-finding method
   code: public static int FindFirstDuplicate(int[] numbers)\n{
  hint2 guide: // guide hints: Create a HashSet to track seen values
   code:     HashSet<int> uniqueNumbers = new HashSet<int>();
  hint3 guide: // guide hints: Iterate through each number
   code:     foreach (var num in numbers)\n    {
  hint4 guide: // guide hints: Return the number if it already exists
   code:         if (!uniqueNumbers.Add(num))\n        {\n            return num;\n        }
  hint5 guide: // guide hints: Return -1 after the loop if nothing duplicated
   code:     }\n    return -1;\n}

FILE: csharp-linq-filter-array.json
 title: LINQ Array Filtering
 desc: Use LINQ to filter an integer array and return values between two bounds.
 checklist: ['Accept an integer array parameter', 'Use LINQ Where to filter values', 'Define the lower and upper bounds', 'Return the filtered enumerable or array', 'Use a lambda expression in the query']
 starterCode: True
 starterCode snippet:
// 1: Accept an integer array input\n// 2: Use LINQ Where with a lambda predicate\n// 3: Filter numbers within the bounds\n// 4: Return the filtered result
 hints count: 3
  hint1 guide: // guide hints: Define the LINQ filter method
   code: public static IEnumerable<int> FilterNumbers(int[] numbers, int minValue, int maxValue)\n{
  hint2 guide: // guide hints: Use Where to filter values
   code:     var filteredNumbers = numbers.Where(n => n > minValue && n < maxValue);
  hint3 guide: // guide hints: Return the filtered collection
   code:     return filteredNumbers;\n}

FILE: csharp-multithreading.json
 title: Multithreading Example
 desc: Write a method that starts a background thread, runs a simple action, and waits for it to complete.
 checklist: ['Import System.Threading or use Thread directly', 'Create a new Thread with a lambda or ThreadStart', 'Start the thread', 'Wait for the thread to finish using Join', 'Execute a simple action inside the thread']
 starterCode: True
 starterCode snippet:
// 1: Create a thread with a lambda or ThreadStart delegate\n// 2: Start the thread\n// 3: Join the thread to wait for completion\n// 4: Return after the work is done
 hints count: 4
  hint1 guide: // guide hints: Define the multithreading method
   code: public static void MultithreadingExample()\n{
  hint2 guide: // guide hints: Create a new thread with a lambda
   code:     Thread thread = new Thread(() =>\n    {\n        // Work to do in the background\n    });
  hint3 guide: // guide hints: Start the thread and wait for it to finish
   code:     thread.Start();\n    thread.Join();
  hint4 guide: // guide hints: Close the method
   code: }

FILE: csharp-palindrome-check.json
 title: Palindrome Check
 desc: Build a method that returns true when the input string reads the same forward and backward.
 checklist: ['Accept a string parameter', 'Normalize or trim the input if needed', 'Compare the input to its reversed version', 'Return true for palindromes', 'Return false otherwise']
 starterCode: True
 starterCode snippet:
// 1: Accept a string input\n// 2: Reverse the string or compare to its reverse\n// 3: Return true when the string is a palindrome\n// 4: Return false otherwise
 hints count: 2
  hint1 guide: // guide hints: Define the palindrome check method
   code: public static bool IsPalindrome(string input)\n{
  hint2 guide: // guide hints: Compare input with reversed sequence
   code:     return input.SequenceEqual(input.Reverse());\n}

FILE: csharp-reverse-string.json
 title: Reverse a String
 desc: Write a method that returns a reversed copy of the input string.
 checklist: ['Accept a string parameter', 'Convert the string to a char array', 'Reverse the char array', 'Create a new string from the reversed array', 'Return the reversed string']
 starterCode: True
 starterCode snippet:
// 1: Accept a string input parameter\n// 2: Convert input to a char array\n// 3: Reverse the char array\n// 4: Return a new string based on the reversed array
 hints count: 4
  hint1 guide: // guide hints: Define the reverse string method
   code: public static string ReverseString(string input)\n{
  hint2 guide: // guide hints: Convert the input to a char array
   code:     char[] charArray = input.ToCharArray();
  hint3 guide: // guide hints: Reverse the char array
   code:     Array.Reverse(charArray);
  hint4 guide: // guide hints: Create a new string and return it
   code:     return new string(charArray);\n}

FILE: csharp-singleton-pattern.json
 title: Singleton Design Pattern
 desc: Create a thread-safe singleton class with lazy initialization.
 checklist: ['Create a sealed Singleton class', 'Add a private static instance field', 'Add a private static readonly lock object', 'Use a private constructor', 'Implement a public Instance property with locking']
 starterCode: True
 starterCode snippet:
// 1: Create a sealed Singleton class\n// 2: Use a private static instance field and lock object\n// 3: Make the constructor private\n// 4: Return a singleton instance in the Instance property
 hints count: 4
  hint1 guide: // guide hints: Define the sealed singleton class
   code: public sealed class Singleton\n{
  hint2 guide: // guide hints: Add a private static instance and lock object
   code:     private static Singleton instance;\n    private static readonly object lockObject = new object();
  hint3 guide: // guide hints: Make the constructor private
   code:     private Singleton()\n    {\n    }
  hint4 guide: // guide hints: Implement the lazy Instance property
   code:     public static Singleton Instance\n    {\n        get\n        {\n            lock (lockObject)\n            {\n                return instance ?? (instance = new Singleton());\n            }\n        }\n    }\n}

FILE: react-counter-advanced.json
 title: Counter Variations
 desc: Extend a counter with step control using Math.max/min, reset, multiple counters, and double/triple increment buttons.
 checklist: ['Import React and useState from React', 'Create a Counter component with count and step state', 'Use Math.max and Math.min to enforce counter limits', 'Add reset, double, and triple actions', 'Render multiple counter instances in App', 'Export default App']
 starterCode: False
 hints count: 6
  hint1 guide: // hint1: Import React and useState from React
   code: import React, { useState } from 'react';
  hint2 guide: // hint2: Create a reusable Counter component and declare required state variables
   code: const CounterComponent = () => {\n  const [count, setCount] = useState(0);\n  const [step, setStep] = useState(1);
  hint3 guide: // hint3: Use Math.max() and Math.min() to enforce counter limits
   code: const increase = () => {\n  setCount(Math.min(count + step, 100));\n};\n\nconst decrease = () => {\n  setCount(Math.max(count - step, 0));\n};
  hint4 guide: // hint4: Create UI for the counter, step control, and action buttons
   code: return (\n  <div>\n    <h2>Count: {count}</h2>\n    <label>Step:<input type="number" value={step} onChange={(e) => setStep(Number(e.target.value))} /></label>\n    <button onClick={increase}>Increase</button>\n    <button onClick={decrease}>Decrease</button>\n    <button onClick={() => setCount(0)}>Reset</button>\n    <button onClick={() => setCount(Math.min(count + step * 2, 100))}>Double</button>\n    <button onClick={() => setCount(Math.min(count + step * 3, 100))}>Triple</button>\n  </div>\n
  hint5 guide: // hint5: Render multiple counter instances
   code: const App = () => {\n  return (\n    <div>\n      <CounterComponent />\n      <CounterComponent />\n    </div>\n  );\n};
  hint6 guide: // hint6: Export the main App component
   code: export default App;

FILE: react-counter-redux.json
 title: Counter with Redux
 desc: Wire a counter to Redux using combineReducers, a counterReducer, useSelector, and useDispatch.
 checklist: ['Import React and react-redux hooks', 'Create counterReducer with INCREMENT and DECREMENT', 'Combine reducers with combineReducers', 'Read count via useSelector', 'Dispatch INCREMENT and DECREMENT with useDispatch', 'Render count and increment/decrement buttons', 'Export default CounterComponent']
 starterCode: True
 starterCode snippet:
// 1: Import: import React from 'react';\n// 2: Import: import { useSelector, useDispatch } from 'react-redux';\n\n// counterReducer.js\n// 3: initialState { count: 0 }, reducer with INCREMENT / DECREMENT\n\n// reducers/index.js\n// 4: combineReducers({ counter: counterReducer })\n\n// CounterComponent.js\nconst CounterComponent = () => {\n  // 5: useSelector(state => state.counter.count)\n  // 6: useDispatch() for INCREMENT / DECREMENT\n  // 7: Render count with Increment / Decrement buttons\n\n  return (\n   
 hints count: 8
  hint1 guide: // guide hints: Import: import React from 'react';
   code: import React from 'react';
  hint2 guide: // guide hints: Import: useSelector and useDispatch from react-redux
   code: import { useSelector, useDispatch } from 'react-redux';
  hint3 guide: // guide hints: initialState { count: 0 }, reducer with INCREMENT / DECREMENT
   code: const initialState = { count: 0 };\nconst counterReducer = (state = initialState, action) => {\n  switch (action.type) {\n    case 'INCREMENT':\n      return { count: state.count + 1 };\n    case 'DECREMENT':\n      return { count: state.count - 1 };\n    default:\n      return state;\n  }\n};
  hint4 guide: // guide hints: combineReducers({ counter: counterReducer })
   code: const rootReducer = combineReducers({ counter: counterReducer });
  hint5 guide: // guide hints: useSelector(state => state.counter.count)
   code: const count = useSelector((state) => state.counter.count);
  hint6 guide: // guide hints: useDispatch() for INCREMENT / DECREMENT
   code: const dispatch = useDispatch();\nconst increment = () => dispatch({ type: 'INCREMENT' });\nconst decrement = () => dispatch({ type: 'DECREMENT' });
  hint7 guide: // guide hints: Render count with Increment / Decrement buttons
   code: return (\n  <div>\n    <h2>Count: {count}</h2>\n    <button onClick={increment}>Increment</button>\n    <button onClick={decrement}>Decrement</button>\n  </div>\n);
  hint8 guide: // guide hints: Export: export default CounterComponent;
   code: export default CounterComponent;

FILE: react-fetch-data.json
 title: Fetch Data on Button Click
 desc: Fetch API data on button click using useReducer with FETCH_SUCCESS and FETCH_ERROR actions.
 checklist: ['Import useReducer from React', 'Define FETCH_SUCCESS and FETCH_ERROR actions', 'Initial state with data: null and error: null', 'Async fetchData function calls API', 'Dispatch FETCH_SUCCESS with response on success', 'Dispatch FETCH_ERROR with message on failure', 'Render data or error in the UI', 'Export default FetchData']
 starterCode: True
 starterCode snippet:
// 1: Import: import React, { useReducer } from 'react';\n\nconst FETCH_SUCCESS = 'FETCH_SUCCESS';\nconst FETCH_ERROR = 'FETCH_ERROR';\nconst initialState = { data: null, error: null };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    default:\n      return state;\n  }\n}\n\nfunction FetchData() {\n  // 2: async fetchData with try/catch\n  // 3: fetch API, dispatch FETCH_SUCCESS or FETCH_ERROR\n  // 4: Button triggers fetchData\n  // 5: Render state.data and state.error\n\n  return (\n    <div>\n 
 hints count: 7
  hint1 guide: // guide hints: Import: import React, { useReducer } from react
   code: import React, { useReducer } from 'react';
  hint2 guide: // guide hints: Define FETCH_SUCCESS and FETCH_ERROR actions and initial state
   code: const FETCH_SUCCESS = 'FETCH_SUCCESS';\nconst FETCH_ERROR = 'FETCH_ERROR';\nconst initialState = { data: null, error: null };
  hint3 guide: // guide hints: Reducer handles FETCH_SUCCESS and FETCH_ERROR
   code: function reducer(state, action) {\n  switch (action.type) {\n    case FETCH_SUCCESS:\n      return { ...state, data: action.payload, error: null };\n    case FETCH_ERROR:\n      return { ...state, error: action.payload };\n    default:\n      return state;\n  }\n}
  hint4 guide: // guide hints: async fetchData with try/catch
   code: const [state, dispatch] = useReducer(reducer, initialState);\nconst fetchData = async () => {\n  try {\n    const response = await fetch('https://jsonplaceholder.typicode.com/todos/1');\n    const data = await response.json();\n    dispatch({ type: FETCH_SUCCESS, payload: data });\n  } catch (error) {\n    dispatch({ type: FETCH_ERROR, payload: error.message });\n  }\n};
  hint5 guide: // guide hints: Button triggers fetchData
   code: <button onClick={fetchData}>Fetch Data</button>
  hint6 guide: // guide hints: Render state.data and state.error
   code: {state.error && <p>{state.error}</p>}\n{state.data && <pre>{JSON.stringify(state.data, null, 2)}</pre>}
  hint7 guide: // guide hints: Export: export default FetchData;
   code: export default FetchData;

FILE: react-form-input.json
 title: Form Input Handling
 desc: Build a controlled form input with useReducer. Display the typed value live as the user types.
 checklist: ['Import useReducer from React', 'Define SET_INPUT action type', 'Reducer updates input from action.payload', 'Controlled input with value={state.input}', 'onChange dispatches SET_INPUT with e.target.value', 'Display entered text below the input', 'Export default FormInput']
 starterCode: True
 starterCode snippet:
// 1: Import: import React, { useReducer } from 'react';\n\nconst SET_INPUT = 'SET_INPUT';\nconst initialState = { input: '' };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    default:\n      return state;\n  }\n}\n\nfunction FormInput() {\n  // 2: useReducer with reducer and initialState\n  // 3: Controlled input value={state.input}\n  // 4: onChange dispatches SET_INPUT with e.target.value\n  // 5: Show <p>Entered Text: {state.input}</p>\n\n  return (\n    <div>\n    </div>\n  );\n}\n\n// 6: Expor
 hints count: 7
  hint1 guide: // guide hints: Import: import React, { useReducer } from react
   code: import React, { useReducer } from 'react';
  hint2 guide: // guide hints: Define SET_INPUT action type and initialState
   code: const SET_INPUT = 'SET_INPUT';\nconst initialState = { input: '' };
  hint3 guide: // guide hints: Reducer updates input from action.payload
   code: function reducer(state, action) {\n  switch (action.type) {\n    case SET_INPUT:\n      return { input: action.payload };\n    default:\n      return state;\n  }\n}
  hint4 guide: // guide hints: useReducer with reducer and initialState
   code: const [state, dispatch] = useReducer(reducer, initialState);
  hint5 guide: // guide hints: Controlled input with value=state.input
   code: <input value={state.input} onChange={(e) => dispatch({ type: SET_INPUT, payload: e.target.value })} />
  hint6 guide: // guide hints: Show Entered Text below the input
   code: <p>Entered Text: {state.input}</p>
  hint7 guide: // guide hints: Export: export default FormInput;
   code: export default FormInput;

FILE: react-manage-list.json
 title: Manage List of Items
 desc: Add and remove list items using useReducer for the list and useState for the input field.
 checklist: ['Import useReducer and useState from React', 'Define ADD_ITEM and REMOVE_ITEM actions', 'Reducer appends item on ADD_ITEM', 'Reducer filters out item by index on REMOVE_ITEM', 'useState holds current input text', 'Add button dispatches ADD_ITEM and clears input', 'Render list with Remove button per item', 'Export default ManageList']
 starterCode: True
 starterCode snippet:
// 1: Import: import React, { useReducer, useState } from 'react';\n\nconst ADD_ITEM = 'ADD_ITEM';\nconst REMOVE_ITEM = 'REMOVE_ITEM';\nconst initialState = { items: [] };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    default:\n      return state;\n  }\n}\n\nfunction ManageList() {\n  // 2: useReducer for items, useState for input\n  // 3: addItem dispatches ADD_ITEM, clears input\n  // 4: Remove dispatches REMOVE_ITEM with index\n  // 5: Map items to <li> with Remove button\n\n  return (\n   
 hints count: 7
  hint1 guide: // guide hints: Import: import React, { useReducer, useState } from react
   code: import React, { useReducer, useState } from 'react';
  hint2 guide: // guide hints: Define ADD_ITEM and REMOVE_ITEM actions and initialState
   code: const ADD_ITEM = 'ADD_ITEM';\nconst REMOVE_ITEM = 'REMOVE_ITEM';\nconst initialState = { items: [] };
  hint3 guide: // guide hints: Reducer appends item on ADD_ITEM and removes by index on REMOVE_ITEM
   code: function reducer(state, action) {\n  switch (action.type) {\n    case ADD_ITEM:\n      return { items: [...state.items, action.payload] };\n    case REMOVE_ITEM:\n      return { items: state.items.filter((item, index) => index !== action.payload) };\n    default:\n      return state;\n  }\n}
  hint4 guide: // guide hints: useReducer for items and useState for input
   code: const [state, dispatch] = useReducer(reducer, initialState);\nconst [input, setInput] = useState('');
  hint5 guide: // guide hints: addItem dispatches ADD_ITEM and clears input
   code: const addItem = () => {\n  dispatch({ type: ADD_ITEM, payload: input });\n  setInput('');\n};
  hint6 guide: // guide hints: Map items to list with Remove button
   code: <ul>{state.items.map((item, index) => (\n  <li key={index}>\n    {item} <button onClick={() => dispatch({ type: REMOVE_ITEM, payload: index })}>Remove</button>\n  </li>\n))}</ul>
  hint7 guide: // guide hints: Export: export default ManageList;
   code: export default ManageList;

FILE: react-tab-navigation.json
 title: Tab Navigation
 desc: Build tab navigation with useReducer. Switch between Tab1, Tab2, and Tab3 content panels.
 checklist: ['Import useReducer from React', 'Define SELECT_TAB action type', "Initial state selectedTab: 'Tab1'", 'Reducer updates selectedTab from payload', 'Render Tab 1, Tab 2, Tab 3 buttons', 'Each button dispatches SELECT_TAB with tab name', 'Show content matching state.selectedTab', 'Export default TabNavigation']
 starterCode: True
 starterCode snippet:
// 1: Import: import React, { useReducer } from 'react';\n\nconst SELECT_TAB = 'SELECT_TAB';\nconst initialState = { selectedTab: 'Tab1' };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    default:\n      return state;\n  }\n}\n\nfunction TabNavigation() {\n  // 2: useReducer with reducer and initialState\n  // 3: Tab buttons dispatch SELECT_TAB with payload\n  // 4: Conditionally render content for selected tab\n\n  return (\n    <div>\n    </div>\n  );\n}\n\n// 5: Export: export default TabNavigat
 hints count: 7
  hint1 guide: // guide hints: Import: import React, { useReducer } from react
   code: import React, { useReducer } from 'react';
  hint2 guide: // guide hints: Define SELECT_TAB action type and initial state
   code: const SELECT_TAB = 'SELECT_TAB';\nconst initialState = { selectedTab: 'Tab1' };
  hint3 guide: // guide hints: Reducer updates selectedTab from action.payload
   code: function reducer(state, action) {\n  switch (action.type) {\n    case SELECT_TAB:\n      return { selectedTab: action.payload };\n    default:\n      return state;\n  }\n}
  hint4 guide: // guide hints: useReducer with reducer and initialState
   code: const [state, dispatch] = useReducer(reducer, initialState);
  hint5 guide: // guide hints: Tab buttons dispatch SELECT_TAB with tab name
   code: <button onClick={() => dispatch({ type: SELECT_TAB, payload: 'Tab1' })}>Tab1</button>\n<button onClick={() => dispatch({ type: SELECT_TAB, payload: 'Tab2' })}>Tab2</button>\n<button onClick={() => dispatch({ type: SELECT_TAB, payload: 'Tab3' })}>Tab3</button>
  hint6 guide: // guide hints: Conditionally render content for selected tab
   code: {state.selectedTab === 'Tab1' && <div>Tab1 Content</div>}\n{state.selectedTab === 'Tab2' && <div>Tab2 Content</div>}\n{state.selectedTab === 'Tab3' && <div>Tab3 Content</div>}
  hint7 guide: // guide hints: Export: export default TabNavigation;
   code: export default TabNavigation;

FILE: react-toggle-visibility.json
 title: Toggle Visibility
 desc: Toggle message visibility using useReducer with a TOGGLE_VISIBILITY action and conditional rendering.
 checklist: ['Import useReducer from React', 'Define TOGGLE_VISIBILITY action type', 'Set initialState with visible: true', 'Reducer flips visible on TOGGLE_VISIBILITY', 'Conditionally render message when visible', 'Button label switches between Hide and Show', 'Export default ToggleVisibility']
 starterCode: True
 starterCode snippet:
// 1: Import: import React, { useReducer } from 'react';\n\nconst TOGGLE_VISIBILITY = 'TOGGLE_VISIBILITY';\nconst initialState = { visible: true };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    default:\n      return state;\n  }\n}\n\nfunction ToggleVisibility() {\n  // 2: useReducer with reducer and initialState\n  // 3: Conditionally render message when state.visible\n  // 4: Button dispatches TOGGLE_VISIBILITY, label Hide / Show\n\n  return (\n    <div>\n    </div>\n  );\n}\n\n// 5: Export: ex
 hints count: 7
  hint1 guide: // guide hints: Import: import React, { useReducer } from react
   code: import React, { useReducer } from 'react';
  hint2 guide: // guide hints: Define TOGGLE_VISIBILITY action type and initialState
   code: const TOGGLE_VISIBILITY = 'TOGGLE_VISIBILITY';\nconst initialState = { visible: true };
  hint3 guide: // guide hints: Reducer flips visible on TOGGLE_VISIBILITY
   code: function reducer(state, action) {\n  switch (action.type) {\n    case TOGGLE_VISIBILITY:\n      return { visible: !state.visible };\n    default:\n      return state;\n  }\n}
  hint4 guide: // guide hints: useReducer with reducer and initialState
   code: const [state, dispatch] = useReducer(reducer, initialState);
  hint5 guide: // guide hints: Conditionally render message when state.visible
   code: {state.visible && <p>The message is visible.</p>}
  hint6 guide: // guide hints: Button dispatches TOGGLE_VISIBILITY, label Hide / Show
   code: <button onClick={() => dispatch({ type: TOGGLE_VISIBILITY })}>{state.visible ? 'Hide' : 'Show'}</button>
  hint7 guide: // guide hints: Export: export default ToggleVisibility;
   code: export default ToggleVisibility;

FILE: sql-duplicate-records.json
 title: Find Duplicate Records
 desc: Write a SQL query to identify duplicate rows in a table based on one or more columns.
 checklist: ['Select the columns to check for duplicates', 'Use GROUP BY on those columns', 'Add HAVING COUNT(*) > 1 to filter duplicates', 'Return the duplicate values']
 starterCode: True
 starterCode snippet:
-- 1: SELECT the columns to check for duplicates\n-- 2: GROUP BY those columns\n-- 3: HAVING COUNT(*) > 1\n-- 4: Return the duplicate values
 hints count: 4
  hint1 guide: SELECT the columns to check for duplicates
   code: SELECT column1, column2
  hint2 guide: GROUP BY those columns
   code: GROUP BY column1, column2
  hint3 guide: HAVING COUNT(*) > 1
   code: HAVING COUNT(*) > 1
  hint4 guide: Return the duplicate values
   code: SELECT column1, column2, COUNT(*)\nFROM YourTable\nGROUP BY column1, column2\nHAVING COUNT(*) > 1;

FILE: sql-first-name-from-fullname.json
 title: Extract First Name
 desc: Write a SQL query to fetch only the first name from the FullName column.
 checklist: ['Use FullName column from EmployeeDetails', 'Extract the substring before the first space', 'Use CHARINDEX or equivalent string function', 'Return a column named FirstName']
 starterCode: True
 starterCode snippet:
-- 1: Select the first name from FullName\n-- 2: Use LEFT and CHARINDEX\n-- 3: Return FirstName
 hints count: 2
  hint1 guide: Select the first name from FullName
   code: SELECT LEFT(FullName, CHARINDEX(' ', FullName + ' ') - 1) AS FirstName
  hint2 guide: Read from EmployeeDetails table
   code: FROM EmployeeDetails;

FILE: sql-inner-join-employee-salary.json
 title: Employees with Salary Records
 desc: Write a SQL query that returns employee records only when they have a salary record.
 checklist: ['Use EmployeeDetails and EmployeeSalary tables', 'Join the tables using INNER JOIN', 'Match on EmpId', 'Return only employees with salary records']
 starterCode: True
 starterCode snippet:
-- 1: Select employee fields from EmployeeDetails\n-- 2: Inner join EmployeeSalary on EmpId\n-- 3: Return matched employee records
 hints count: 2
  hint1 guide: Select employee columns from EmployeeDetails
   code: SELECT ED.*
  hint2 guide: Use INNER JOIN with EmployeeSalary
   code: FROM EmployeeDetails ED\nINNER JOIN EmployeeSalary ES ON ED.EmpId = ES.EmpId;

FILE: sql-left-join-employee-salary.json
 title: Employee Details with Optional Salary
 desc: Write a query to return employee names and salary, including employees without salary records.
 checklist: ['Select FullName and Salary columns', 'Use LEFT JOIN between EmployeeDetails and EmployeeSalary', 'Join on EmpId', 'Return all employee records even when salary is missing']
 starterCode: True
 starterCode snippet:
-- 1: Select employee name and salary\n-- 2: Use LEFT JOIN on EmpId\n-- 3: Return all employee records
 hints count: 2
  hint1 guide: Select employee name and salary
   code: SELECT ED.FullName, ES.Salary
  hint2 guide: Use LEFT JOIN so employees without salary are included
   code: FROM EmployeeDetails ED\nLEFT JOIN EmployeeSalary ES ON ED.EmpId = ES.EmpId;

FILE: sql-managers.json
 title: Employees Who Are Managers
 desc: Write a query to fetch all employees who are also managers in the EmployeeDetails table.
 checklist: ['Use EmployeeDetails table', 'Check ManagerId values present in the table', 'Return records for employees who appear as managers', 'Avoid duplicate results if necessary']
 starterCode: True
 starterCode snippet:
-- 1: Select all columns from EmployeeDetails\n-- 2: Filter employees whose EmpId appears as a ManagerId\n-- 3: Return manager employee rows
 hints count: 2
  hint1 guide: Select all employee columns
   code: SELECT *
  hint2 guide: Use a subquery to find manager IDs
   code: FROM EmployeeDetails\nWHERE EmpId IN (SELECT ManagerId FROM EmployeeDetails WHERE ManagerId IS NOT NULL);

FILE: sql-min-max-salary.json
 title: Min and Max Salary in One Query
 desc: Write a SQL query to display the employee with minimum and maximum salary in a single query.
 checklist: ['Use subqueries or UNION to obtain min and max salary', 'Select employee name, position, and salary', 'Return both rows in one result set', 'Avoid separate queries for min and max']
 starterCode: True
 starterCode snippet:
-- 1: Use min and max salary in a single query\n-- 2: Select employee name, position, salary\n-- 3: Combine the two results
 hints count: 2
  hint1 guide: Select employee information for minimum salary
   code: SELECT Name, Position, Salary\nFROM employee\nWHERE Salary = (SELECT MIN(Salary) FROM employee)
  hint2 guide: Select employee information for maximum salary
   code: UNION\nSELECT Name, Position, Salary\nFROM employee\nWHERE Salary = (SELECT MAX(Salary) FROM employee);

FILE: sql-project-count.json
 title: Count Employees in Project P1
 desc: Write a SQL query that returns the number of employees working in project 'P1'.
 checklist: ['Use SELECT COUNT(*) to count rows', 'Reference the EmployeeDetails table', "Filter WHERE ProjectName = 'P1'", 'Provide a result alias like AS EmployeeCount']
 starterCode: True
 starterCode snippet:
-- 1: Use SELECT COUNT(*) to count rows\n-- 2: FROM EmployeeDetails\n-- 3: WHERE ProjectName = 'P1'\n-- 4: Add AS EmployeeCount to label result
 hints count: 4
  hint1 guide: Use SELECT COUNT(*) to count rows
   code: SELECT COUNT(*)
  hint2 guide: FROM EmployeeDetails
   code: FROM EmployeeDetails
  hint3 guide: WHERE ProjectName = 'P1'
   code: WHERE ProjectName = 'P1'
  hint4 guide: Add AS EmployeeCount to label result
   code: SELECT COUNT(*) AS EmployeeCount\nFROM EmployeeDetails\nWHERE ProjectName = 'P1';

FILE: sql-project-employee-count-desc.json
 title: Project-wise Employee Count
 desc: Write a SQL query to fetch project-wise employee counts sorted by count descending.
 checklist: ['Group employee salary rows by project', 'Count employees per project', 'Sort results by employee count descending', 'Return project and count columns']
 starterCode: True
 starterCode snippet:
-- 1: Count rows per project in EmployeeSalary\n-- 2: Group by project\n-- 3: Order results by descending count
 hints count: 4
  hint1 guide: Select project and count of EmpId
   code: SELECT Project, COUNT(EmpId) AS EmployeeCount
  hint2 guide: Read from EmployeeSalary table
   code: FROM EmployeeSalary
  hint3 guide: Group by project name
   code: GROUP BY Project
  hint4 guide: Order counts in descending order
   code: ORDER BY EmployeeCount DESC;

FILE: sql-remove-duplicates.json
 title: Remove Duplicate Records
 desc: Write a SQL query to remove duplicate rows from a table without using a temporary table.
 checklist: ['Identify duplicate rows using a ranking or counting method', 'Keep one row per duplicate group', 'Remove rows with a higher duplicate rank', 'Avoid use of a temporary table']
 starterCode: True
 starterCode snippet:
-- 1: Use a CTE with ROW_NUMBER() to identify duplicates\n-- 2: Delete rows with row number > 1\n-- 3: Keep one unique row per group
 hints count: 2
  hint1 guide: Define a CTE to rank duplicate rows
   code: WITH RankedData AS (\n    SELECT EmpId, FullName, ManagerId, DateOfJoining,\n           ROW_NUMBER() OVER (PARTITION BY FullName, ManagerId, DateOfJoining ORDER BY EmpId) AS RowNo\n    FROM EmployeeDetails\n)
  hint2 guide: Delete rows where row number is greater than 1
   code: DELETE FROM RankedData WHERE RowNo > 1;

FILE: sql-salary-range.json
 title: Salary Range Query
 desc: Write a SQL query to fetch employee names whose salary is between 5000 and 10000 inclusive.
 checklist: ['Use EmployeeDetails and EmployeeSalary tables together', 'Join the tables using EmpId', 'Filter salaries >= 5000 and <= 10000', 'Select the employee names', 'Return only matching employee records']
 starterCode: True
 starterCode snippet:
-- 1: Join EmployeeDetails with EmployeeSalary\n-- 2: Filter salary between 5000 and 10000\n-- 3: Select employee names
 hints count: 4
  hint1 guide: Select employee full names from EmployeeDetails
   code: SELECT ED.FullName
  hint2 guide: Join EmployeeSalary with EmployeeDetails by EmpId
   code: FROM EmployeeDetails ED\nJOIN EmployeeSalary ES ON ED.EmpId = ES.EmpId
  hint3 guide: Filter salary between 5000 and 10000
   code: WHERE ES.Salary >= 5000 AND ES.Salary <= 10000
  hint4 guide: Return employee names for matching records
   code: SELECT ED.FullName\nFROM EmployeeDetails ED\nJOIN EmployeeSalary ES ON ED.EmpId = ES.EmpId\nWHERE ES.Salary >= 5000 AND ES.Salary <= 10000;

FILE: sql-second-largest-salary.json
 title: Second Largest Salary
 desc: Write a SQL query to fetch the second highest salary using ranking or OFFSET.
 checklist: ['Query the employee salary values', 'Use RANK() or OFFSET to find the second highest salary', 'Return the second largest salary row', 'Avoid using procedural code']
 starterCode: True
 starterCode snippet:
-- 1: Use ranking or OFFSET to locate the second largest salary\n-- 2: Return the salary value or row\n-- 3: Keep the query in a single statement
 hints count: 2
  hint1 guide: Rank salaries in descending order
   code: WITH RankedSalaries AS (\n    SELECT EmployeeID, Salary, RANK() OVER (ORDER BY Salary DESC) AS SalaryRank\n    FROM Employees\n)
  hint2 guide: Select the row with rank 2
   code: SELECT EmployeeID, Salary\nFROM RankedSalaries\nWHERE SalaryRank = 2;

